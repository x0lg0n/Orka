---
phase: 01-rust-backend-services
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/Cargo.toml
  - services/src/main.rs
  - services/src/config.rs
  - services/src/state.rs
  - services/src/router.rs
  - services/src/auth.rs
  - services/src/custody.rs
  - services/src/stellar.rs
  - services/src/bridge.rs
autonomous: true
requirements:
  - PH1-01
must_haves:
  truths:
    - "GET /health returns HTTP 200 with body 'ok'"
    - "Server starts and reads all six required env vars (STELLAR_RPC_URL, STELLAR_NETWORK, ORKA_OPERATOR_SECRET, KMS_CONFIG, SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY) without panicking"
    - "`cargo build --manifest-path services/Cargo.toml` exits 0 (workspace compiles)"
  artifacts:
    - services/Cargo.toml
    - services/src/main.rs
    - services/src/config.rs
    - services/src/state.rs
    - services/src/router.rs
    - services/src/auth.rs (stub returning Router::new())
    - services/src/custody.rs (stub returning Router::new())
    - services/src/stellar.rs (stub returning Router::new())
    - services/src/bridge.rs (stub returning Router::new())
  key_links:
    - "main.rs declares `pub mod auth; pub mod custody; pub mod stellar; pub mod bridge;` and mounts router::build_router(state)"
    - "AppState is shared across handlers via axum's State extractor"
---

<objective>
Scaffold the `services/` Rust (Axum) workspace so that Phase 1 plans 01-02..01-05 have a compiling home and a shared runtime (config + app state + router). This satisfies Phase 1 success criterion 1 (PH1-01): "`services/` compiles and runs an Axum server exposing escrow orchestration endpoints."

Purpose: A green-compiling skeleton lets each downstream plan own one module file (auth.rs / custody.rs / stellar.rs / bridge.rs) without colliding on `main.rs`.
Output: A runnable Axum server with a `/health` endpoint and stub route modules.
</objective>

<execution_context>
@C:/Users/kumar/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/kumar/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@C:/Siddhartha/ORKA/Orka/.planning/ROADMAP.md
@C:/Siddhartha/ORKA/Orka/ROADMAP.md
@C:/Siddhartha/ORKA/Orka/frontend/lib/supabase.ts
@C:/Siddhartha/ORKA/Orka/frontend/supabase/phase1_schema.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cargo.toml + Config + AppState + health endpoint</name>
  <files>services/Cargo.toml, services/src/config.rs, services/src/state.rs, services/src/main.rs, services/src/router.rs</files>
  <read_first>frontend/lib/supabase.ts, frontend/supabase/phase1_schema.sql</read_first>
  <objective>Establish the crate, load env config, build shared AppState, and expose GET /health.</objective>
  <action>
    Create `services/Cargo.toml` as package `orka-services` (edition 2021) depending on: `axum` (0.7), `tokio` (1, full), `tower` + `tower-http` (cors, trace), `serde` + `serde_json`, `dotenvy`, `reqwest` (0.12, json, rustls-tls), `thiserror`, `tracing` + `tracing-subscriber`, `stellar-xdr` (std), `stellar-strkey`, `ed25519-dalek` (2), `jsonwebtoken` (9). Keep versions loose (e.g. `axum = "0.7"`).
    Create `services/src/config.rs`:
      - `pub struct Config { pub stellar_rpc_url: String, pub stellar_network: String, pub orka_operator_secret: String, pub kms_config: String, pub supabase_url: String, pub supabase_service_role_key: String, pub port: u16 }`.
      - `Config::from_env() -> Result<Config, config::Error>` using `dotenvy::dotenv().ok()` then `std::env::var`. Provide sane defaults for `port` (3000) and `stellar_network` ("testnet") so unit tests do not require real values. Return a typed error (define `pub enum Error` with `thiserror`). Do NOT log secret values.
    Create `services/src/state.rs`:
      - `pub struct Supabase { pub client: reqwest::Client, pub base_url: String, pub key: String }` with `impl Supabase { pub fn from_config(c:&Config) -> Self }` (base_url = `{supabase_url}/rest/v1`, Authorization/API key header from key). Keep query-building minimal here; full upserts land in 01-05.
      - `pub struct AppState { pub config: Config, pub http: reqwest::Client, pub supabase: Supabase }` plus `impl AppState { pub fn new(config: Config) -> Self }`.
    Create `services/src/router.rs`: `pub fn build_router(state: AppState) -> axum::Router` that merges `axum::Router::new().route("/health", get(health))` with `auth::routes(&state)`, `custody::routes(&state)`, `stellar::routes(&state)`, `bridge::routes(&state)`, wraps with `.with_state(state)` and `CorsLayer::permissive()` (tighten later). `async fn health() -> &'static str { "ok" }`.
    Create `services/src/main.rs`: `pub mod auth; pub mod custody; pub mod stellar; pub mod bridge; pub mod config; pub mod state; pub mod router;`, load Config, build AppState, `axum::serve` on `0.0.0.0:port`, with `tracing_subscriber::fmt::init()`.
    Add a `#[cfg(test)] mod tests { #[test] fn config_parses_with_defaults() { let c = Config::from_env().unwrap(); assert!(!c.supabase_url.is_empty()); } }` gated so it only runs when env absent (use a fallback default path).
  </action>
  <acceptance_criteria>
    - `cargo build --manifest-path services/Cargo.toml` exits 0.
    - `Config` contains exactly the six required vars plus port.
    - `AppState` is constructible from `Config`.
    - A `#[test]` exists asserting config parses.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path services/Cargo.toml config 2>&1 | tail -20</automated>
  </verify>
  <done>Workspace compiles; config loads; at least one passing unit test.</done>
</task>

<task type="auto">
  <name>Task 2: Stub route modules so the workspace compiles</name>
  <files>services/src/auth.rs, services/src/custody.rs, services/src/stellar.rs, services/src/bridge.rs</files>
  <read_first>services/src/router.rs, services/src/state.rs</read_first>
  <objective>Provide minimal `routes()` fns in each module so `build_router` compiles; downstream plans (01-02..01-05) replace the bodies.</objective>
  <action>
    For each of auth.rs, custody.rs, stellar.rs, bridge.rs create a file with:
      `use crate::state::AppState; use axum::Router;`
      `pub fn routes(_state: &AppState) -> Router { Router::new() }`
    Do NOT implement handlers yet — only satisfy the `pub fn routes(&AppState) -> Router` signature referenced by `router.rs`. These are intentional stubs replaced by 01-02..01-05 (cross-wave edit, allowed).
  </action>
  <acceptance_criteria>
    - `cargo build --manifest-path services/Cargo.toml` exits 0 with all four stub modules present.
    - Each module exposes `pub fn routes(&AppState) -> Router`.
    - `cargo run --manifest-path services/Cargo.toml` (after build) serves `/health` returning `ok`.
  </acceptance_criteria>
  <verify>
    <automated>cargo build --manifest-path services/Cargo.toml 2>&1 | tail -20</automated>
    <human-check>Run `cargo run --manifest-path services/Cargo.toml` then `curl -s localhost:3000/health` returns `ok`.</human-check>
  </verify>
  <done>Four stub modules compile and the server answers /health.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | Public HTTP surface (only /health is live in this plan) |
| env→process | Secrets (ORKA_OPERATOR_SECRET, SUPABASE_SERVICE_ROLE_KEY, KMS_CONFIG) loaded from env |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Spoofing | /health (public) | low | accept | Health is non-sensitive; no auth needed |
| T-01-02 | Tampering | env/config load | medium | mitigate | Validate all six required vars; fail fast with clear error; never print secret values (no `{:?}` on Config) |
| T-01-03 | Information Disclosure | logs | medium | mitigate | tracing must not serialize Config or secrets; CORS permissive only for scaffold, tighten in later plan |
| T-01-SC | Tampering | cargo installs | high | mitigate | Use only crates pinned in Cargo.toml; no `[ASSUMED]`/external install scripts; blocking human checkpoint if a new crate is added |
</threat_model>

<verification>
- `cargo build --manifest-path services/Cargo.toml` exits 0.
- `cargo test --manifest-path services/Cargo.toml` has at least one passing config test.
- Server boots and `GET /health` returns 200 `ok`.
</verification>

<success_criteria>
`services/` compiles as an Axum package, reads all six required env vars, and serves a working `/health` endpoint, with stub modules auth.rs/custody.rs/stellar.rs/bridge.rs ready for downstream plans.
</success_criteria>

<output>
Create `.planning/phases/01-rust-backend-services/01-01-SUMMARY.md` when done.
</output>
