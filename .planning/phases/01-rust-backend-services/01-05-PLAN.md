---
phase: 01-rust-backend-services
plan: 05
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - services/src/bridge.rs
autonomous: true
requirements:
  - PH1-05
must_haves:
  truths:
    - "applyChainEvent() is the SINGLE reconciler: it upserts ledger_events AND updates milestones.status"
    - "No other module in services/ writes milestone status directly — every status change flows through the bridge"
    - "Reconciliation is idempotent (replaying the same chain_tx does not create duplicate ledger rows)"
  artifacts:
    - services/src/bridge.rs (full implementation replacing the stub)
    - `pub trait LedgerSink` (Supabase-backed impl + Mock for tests)
  key_links:
    - "UI reads only from Postgres (per root ROADMAP §Guiding Principles #2); bridge keeps Postgres in lockstep with chain"
    - "event_type + MilestoneStatus map deterministically to the Postgres milestone_status enum"
---

<objective>
Implement `bridge.rs`: the `applyChainEvent()` reconciler that upserts `ledger_events` and updates `milestones.status` in Supabase. Satisfies Phase 1 success criterion 5 (PH1-05): "`bridge.rs` `applyChainEvent()` upserts `ledger_events` and updates `milestones.status`."

Purpose: Enforce root ROADMAP §Guiding Principles #2 — one sync bridge, no feature writes milestone status directly.
Output: `bridge.rs` with `apply_chain_event`, the `LedgerSink` trait (Supabase + Mock), the status mapping, and `POST /bridge/event`.
</objective>

<execution_context>
@C:/Users/kumar/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/kumar/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@C:/Siddhartha/ORKA/Orka/ROADMAP.md
@C:/Siddhartha/ORKA/Orka/frontend/supabase/phase1_schema.sql
@C:/Siddhartha/ORKA/Orka/contracts/escrow/src/types.rs
@C:/Siddhartha/ORKA/Orka/services/src/state.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: ChainEvent model + LedgerSink trait (Supabase + Mock)</name>
  <files>services/src/bridge.rs</files>
  <read_first>frontend/supabase/phase1_schema.sql, services/src/state.rs</read_first>
  <objective>Define the event model and a `LedgerSink` trait so the reconciler is testable without live Supabase.</objective>
  <action>
    Define `pub struct ChainEvent { pub org_id: String, pub project_id: String, pub milestone_id: Option<String>, pub chain_tx: String, pub event_type: EventType, pub amount: Option<f64>, pub asset: Option<String>, pub onchain_status: MilestoneStatus }` where `EventType` ∈ {fund, submit, approve, release, refund, dispute, resolve, initialize} and `MilestoneStatus` mirrors `contracts/escrow/src/types.rs` (Draft..Released as u32/i32).
    Define `pub trait LedgerSink: Send + Sync { async fn upsert_event(&self, e: &ChainEvent) -> Result<(), BridgeError>; async fn update_milestone_status(&self, milestone_id: &str, status: &str) -> Result<(), BridgeError>; }`.
    Implement `pub struct SupabaseSink { supabase: crate::state::Supabase }` whose `upsert_event` does `POST {base_url}/ledger_events` with `Prefer: resolution=merge-duplicates` keyed by `chain_tx` (idempotent upsert), and `update_milestone_status` does `PATCH {base_url}/milestones?id=eq.{milestone_id}` with `{"status": ...}`.
    Implement `pub struct MockSink { events: Mutex<Vec<ChainEvent>>, statuses: Mutex<HashMap<String,String>> }` for tests (records calls, asserts).
    Define `pub enum BridgeError` (thiserror): `Supabase`, `MissingMilestone`, `InvalidStatus`, `Serialize`.
  </action>
  <acceptance_criteria>
    - `LedgerSink` trait compiles; both `SupabaseSink` and `MockSink` implement it.
    - `upsert_event` uses `Prefer: resolution=merge-duplicates` (idempotent on chain_tx).
    - Unit test confirms `MockSink` records the event + status update.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path services/Cargo.toml bridge::tests::sink 2>&1 | tail -20</automated>
  </verify>
  <done>LedgerSink trait + Supabase/Mock impls compile; idempotent upsert confirmed in test.</done>
</task>

<task type="auto">
  <name>Task 2: apply_chain_event reconciler + status mapping</name>
  <files>services/src/bridge.rs</files>
  <read_first>services/src/bridge.rs, contracts/escrow/src/types.rs, frontend/supabase/phase1_schema.sql</read_first>
  <objective>Implement the single reconciler and the explicit on-chain→Postgres status mapping.</objective>
  <action>
    Implement `pub async fn apply_chain_event(sink: &dyn LedgerSink, e: &ChainEvent) -> Result<(), BridgeError>` that:
      (1) maps `e.onchain_status` → Postgres `milestone_status` via an explicit, documented function `map_status(MilestoneStatus) -> &'static str`:
          - Draft -> "draft"
          - Funded -> "funded"
          - Submitted -> "in_review"
          - Approved -> "in_review"   (Postgres enum has no 'approved'; client approval is reflected at release; document this)
          - Rejected -> "in_review"   (rejected returns to review; document)
          - Refunded -> "refunded"
          - Disputed -> "disputed"
          - Released -> "released"
          (If the product later needs a distinct 'submitted'/'approved' state, extend the Postgres enum — note this in code comment. DO NOT invent a status not in the enum.)
      (2) `sink.upsert_event(e)` (ledger_events row with event_type/amount/asset/status).
      (3) if `e.milestone_id.is_some()` → `sink.update_milestone_status(id, map_status(...))`.
      This is THE only path that writes milestone status; add a code comment + test asserting no other module does.
    Add a unit test `map_status_covers_all` iterating all 8 `MilestoneStatus` variants and asserting a non-empty Postgres string, and `apply_updates_status` using `MockSink`.
  </action>
  <acceptance_criteria>
    - All 8 `MilestoneStatus` values map to a valid Postgres `milestone_status` string.
    - `apply_chain_event` calls both `upsert_event` and `update_milestone_status` exactly once per call.
    - Idempotent: calling twice with same `chain_tx` does not duplicate the ledger row (MockSink count assertion).
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path services/Cargo.toml bridge::tests::apply 2>&1 | tail -20</automated>
  </verify>
  <done>apply_chain_event reconciles both tables; status mapping exhaustive and tested; idempotent.</done>
</task>

<task type="auto">
  <name>Task 3: Route wiring (POST /bridge/event)</name>
  <files>services/src/bridge.rs</files>
  <read_first>services/src/bridge.rs, services/src/router.rs</read_first>
  <objective>Expose the reconciler over HTTP, auth-guarded, replacing the stub routes().</objective>
  <action>
    Implement `pub fn routes(state: &AppState) -> Router` (replacing stub) mounting `POST /bridge/event` (JSON body = `ChainEvent`, validated). The handler should be behind auth (require a verified `AuthenticatedUser` from `auth` — add an `axum::extract::State<AppState>` + a check that the caller is authorized for `org_id`; for Phase 1 a simple service-role internal gate is acceptable, documented as temporary). Build a `SupabaseSink` from `state.supabase` and call `apply_chain_event`. Return 200 on success.
    Add a unit/integration test using `MockSink` wired through the router (via `axum::Router::into_service`) asserting `POST /bridge/event` returns 200 and records the event.
  </action>
  <acceptance_criteria>
    - `POST /bridge/event` with a valid `ChainEvent` returns 200 and triggers reconciliation.
    - Route is wired into `router.rs` automatically (build_router already calls bridge::routes).
    - `cargo build --manifest-path services/Cargo.toml` exits 0 with no other module writing milestone status.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path services/Cargo.toml bridge 2>&1 | tail -20</automated>
  </verify>
  <done>applyChainEvent exposed as POST /bridge/event; bridge is the single sync path; full crate builds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller→/bridge/event | Reconciler entrypoint (should be internal/service-only) |
| bridge→Supabase | ledger_events + milestones writes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-05-01 | Tampering | direct milestone write | high | mitigate | `apply_chain_event` is the ONLY writer of milestone status; document + assert no other module writes it |
| T-01-05-02 | Spoofing | /bridge/event open | high | mitigate | Gate behind auth/service-role; internal-only for Phase 1 (documented temporary) |
| T-01-05-03 | Tampering | duplicate ledger rows | medium | mitigate | Idempotent upsert keyed by chain_tx (`Prefer: resolution=merge-duplicates`) |
| T-01-05-04 | Information Disclosure | DB errors | low | mitigate | Do not leak Supabase errors to clients; log server-side only |
| T-01-SC | Tampering | cargo installs | high | mitigate | Pin `reqwest`; reuses crate set from 01-01; blocking human checkpoint on new crate |
</threat_model>

<verification>
- `cargo test --manifest-path services/Cargo.toml bridge` all green.
- `apply_chain_event` is the single reconciler; status mapping exhaustive; idempotent upsert confirmed.
</verification>

<success_criteria>
`bridge.rs` `applyChainEvent()` is the single reconciler that upserts ledger_events and updates milestones.status, with an explicit on-chain→Postgres status mapping and idempotency — satisfying PH1-05.
</success_criteria>

<output>
Create `.planning/phases/01-rust-backend-services/01-05-SUMMARY.md` when done.
</output>
