---
phase: 01-rust-backend-services
plan: 04
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - services/src/stellar.rs
autonomous: true
requirements:
  - PH1-04
must_haves:
  truths:
    - "stellar.rs builds a Soroban `InvokeContract` tx matching the escrow contract's exact function signatures"
    - "Every tx is wrapped in an ORKA-operator-sponsored `fee_bump` (zero gas for the user in both modes)"
    - "`release_funds` carries multi-sig auth (client key + operator key); a single leaked key cannot release"
    - "Mode B routes return the unsigned/partially-built tx XDR for Freighter to sign — backend never signs the user key"
  artifacts:
    - services/src/stellar.rs (full implementation replacing the stub)
    - RPC submit helper + escrow function mappers
  key_links:
    - "operator key = ORKA_OPERATOR_SECRET; it is the fee_bump payer AND the co-signer for release_funds"
    - "Mode A signing delegates to custody::sign_for_user; Mode B returns XDR without user signature"
---

<objective>
Implement `stellar.rs`: Stellar SDK client that builds escrow txns, wraps them in an operator-sponsored `fee_bump`, and submits to RPC; supports multi-sig `release_funds` and Mode B serialization. Satisfies Phase 1 success criterion 4 (PH1-04): "`stellar.rs` builds + submits a sponsored `fee_bump` tx to Stellar RPC."

Purpose: Real chain submission with zero user gas (root ROADMAP §Auth & Custody Model) and multi-sig release (§Guiding Principles).
Output: `stellar.rs` with `build_contract_tx`, `sponsor_and_submit`, `build_tx_for_freighter`, escrow fn mappers, and routes `POST /escrow/fund`, `POST /escrow/release`, `GET /escrow/state`.
</objective>

<execution_context>
@C:/Users/kumar/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/kumar/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@C:/Siddhartha/ORKA/Orka/contracts/escrow/src/lib.rs
@C:/Siddhartha/ORKA/Orka/contracts/escrow/src/types.rs
@C:/Siddhartha/ORKA/Orka/contracts/escrow/src/errors.rs
@C:/Siddhartha/ORKA/Orka/services/src/state.rs
@C:/Siddhartha/ORKA/Orka/services/src/config.rs
@C:/Siddhartha/ORKA/Orka/services/src/custody.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tx builder + escrow function mappers</name>
  <files>services/src/stellar.rs</files>
  <read_first>contracts/escrow/src/lib.rs, contracts/escrow/src/types.rs, services/src/config.rs</read_first>
  <objective>Build Soroban InvokeContract operations for every escrow function, using `stellar-xdr` types that match the contract's exact signatures.</objective>
  <action>
    Use `stellar-xdr` (feature `std`) for `Transaction`, `Operation`, `InvokeContractArgs`, `HostFunction`, `ScVal`, `ScAddress`, `Uint256`, `Int128`; `stellar-strkey` to parse contract `C...` and account `G...` addresses into `ScAddress`; `ed25519-dalek` for signing; `reqwest` for RPC.
    Implement `pub struct EscrowCall` and `pub fn build_contract_tx(cfg: &Config, contract_id: &str, fn_name: &str, args: Vec<ScVal>, source_account: &str, sequence: i64, fee: u32) -> Result<TransactionEnvelope, StellarError>` that constructs an `Operation` with `HostFunction::InvokeContract(InvokeContractArgs { contract_address, function_name: fn_name.into(), args })`, sets `source_account` (parsed to `MuxedAccount`), `fee`, `seq_num = sequence`, empty `cond`, `1` op.
    Provide mappers that translate each escrow function (per `contracts/escrow/src/lib.rs`) to `args`:
      - `initialize(org:Bytes, client:Address, freelancer:Address, asset:Address, operator:Address, milestones:Vec<MilestoneInit>, dispute_rules:Option<u32>)` — note `org` is `Bytes` (use `ScVal::Bytes` from the org id bytes), others `ScAddress::Account`, milestones as `Vec<ScVal>` of `{amount:i128}` structs, dispute_rules `Option`.
      - `fund(milestone_ids: Vec<u64>)`, `submit(milestone_id:u64)`, `approve(u64)`, `reject(u64)`, `refund(u64)`, `open_dispute(caller:Address, u64)`, `release_funds(u64)`, `resolve_dispute(u64, Option<u32>)`.
    Map `MilestoneStatus` values (Draft=0..Released=7) to `ScVal::U32` for any status-bearing call (not needed for these fns; only `resolve_dispute` split_bp uses u32/bp).
    Define `pub enum StellarError` (thiserror): `Xdr`, `Address`, `Rpc`, `Sign`, `Seq`.
  </action>
  <acceptance_criteria>
    - `build_contract_tx` produces a `TransactionEnvelope` whose single operation is `InvokeContract` for the requested `fn_name` with correctly-typed `ScVal` args.
    - A unit test `build_fund_tx` asserts the envelope parses back and the operation's `function_name` == "fund".
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path services/Cargo.toml stellar::tests::build 2>&1 | tail -20</automated>
  </verify>
  <done>Tx builder + escrow mappers compile; unit tests assert envelope structure for fund/release.</done>
</task>

<task type="auto">
  <name>Task 2: Sponsored fee_bump + submit + multi-sig release</name>
  <files>services/src/stellar.rs</files>
  <read_first>services/src/stellar.rs, services/src/custody.rs, contracts/escrow/src/lib.rs</read_first>
  <objective>Wrap any inner tx in an operator fee_bump, sign with ORKA_OPERATOR_SECRET, submit to RPC; implement multi-sig for release_funds.</objective>
  <action>
    Implement `pub fn sponsor_and_submit(cfg: &Config, inner: Transaction, operator_secret: &str, user_signatures: Vec<(DecoratedSignature)> ) -> Result<String, StellarError>`:
      - Build `FeeBumpTransaction { inner_tx: EnvelopeType::Tx(inner), fee_source: operator AccountID (from ORKA_OPERATOR_SECRET), fee: cfg-based bump fee }`.
      - Attach `user_signatures` as the inner tx's `signatures` (for Mode A these are produced by `custody::sign_for_user` over the inner tx hash; for Mode B by Freighter — but in submit path the caller passes them in).
      - Sign the `FeeBumpTransaction` with the operator key (ed25519 over the fee_bump hash) → add `DecoratedSignature` to `fee_bump.signatures`.
      - Serialize to base64 XDR and `POST {cfg.stellar_rpc_url}/` method `sendTransaction` via `reqwest` JSON-RPC; return `tx_hash` from the response (or poll `getTransaction` for status).
    Implement `pub async fn release_funds_sponsored(state, project, milestone_id, client_user_id, kms)` (Mode A orchestration): build inner `release_funds(milestone_id)` with `source_account = client address`, then sign inner with operator key (operator `require_auth`) AND client key via `custody::sign_for_user` (client `require_auth`) → two `DecoratedSignature`s on the inner tx (multi-sig). Then fee_bump-sign with operator and submit. Assert two auth signatures present.
    Document: `release_funds` REQUIRES both client + operator `require_auth` (see lib.rs lines 188-190); a single leaked key cannot release.
  </action>
  <acceptance_criteria>
    - `sponsor_and_submit` returns a tx hash from a (test) RPC or, when offline, a unit test asserts the fee_bump XDR parses to a `FeeBumpTransaction` with operator as `fee_source`.
    - Multi-sig test asserts the inner tx carries exactly 2 `DecoratedSignature`s for release_funds (client + operator).
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path services/Cargo.toml stellar::tests::feebump 2>&1 | tail -20</automated>
  </verify>
  <done>fee_bump sponsorship + multi-sig release compile and unit tests pass (no live RPC required for tests).</done>
</task>

<task type="auto">
  <name>Task 3: Mode B serialization + route wiring</name>
  <files>services/src/stellar.rs</files>
  <read_first>services/src/stellar.rs, services/src/auth.rs</read_first>
  <objective>Mode B: return the built (un-signed user side) tx XDR for Freighter; backend never signs the user key. Wire escrow routes.</objective>
  <action>
    Implement `pub fn build_tx_for_freighter(cfg: &Config, contract_id: &str, fn_name: &str, args: Vec<ScVal>, user_address: &str, sequence: i64) -> Result<String, StellarError>`:
      - Build the inner tx with `source_account = user_address` (the Freighter key), wrap in operator `fee_bump` (operator is fee payer), sign ONLY the fee_bump with the operator key, and return the base64 XDR string. The inner tx's user signature is left for Freighter to add in-browser. This is the Mode B path that "skips custody.rs and returns a tx for Freighter to sign".
    Implement `pub fn routes(state: &AppState) -> Router` (replacing stub) mounting:
      - `POST /escrow/fund` (body `{contract_id, milestone_ids, mode}`) → Mode A: sponsor_and_submit with custody sign; Mode B: return `{tx_xdr}` from build_tx_for_freighter.
      - `POST /escrow/release` (body `{contract_id, milestone_id, mode, user_id?}`) → multi-sig for Mode A; tx_xdr for Mode B.
      - `GET /escrow/state?contract_id=` → fetch contract state via RPC `getLedgerEntries` for the contract's Milestones instance (best-effort; return milestone statuses).
      Each handler MUST first call `auth::assert_signing_allowed` (or accept a pre-verified `AuthenticatedUser`) so Mode B handlers never invoke custody signing.
    Add a unit test `mode_b_returns_xdr_without_user_sig` asserting the returned XDR parses to a fee_bump whose inner tx has zero user signatures.
  </action>
  <acceptance_criteria>
    - `build_tx_for_freighter` returns base64 XDR of a fee_bump with operator fee source and an inner tx carrying no user signature.
    - `/escrow/fund` and `/escrow/release` branch on `mode`: Mode A signs via custody, Mode B returns `tx_xdr`.
    - Mode B route never calls `custody::sign_for_user`.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path services/Cargo.toml stellar 2>&1 | tail -20</automated>
  </verify>
  <done>Mode B XDR serialization + escrow routes compile and tests pass; fee_bump zero-gas model verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend→Stellar RPC | sendTransaction JSON-RPC |
| custody→stellar | sign_for_user (Mode A only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-04-01 | Tampering | tx integrity | high | mitigate | Inner tx hashes are signed (ed25519) before fee_bump wrap; verify signature counts in tests |
| T-01-04-02 | Spoofing | multi-sig release | high | mitigate | release_funds requires client+operator auth; assert 2 signatures in unit test |
| T-01-04-03 | Elevation | Mode B sign | critical | mitigate | Mode B handlers return XDR and MUST NOT call custody::sign_for_user (enforced by test T-01-03-04 + route design) |
| T-01-04-04 | Information Disclosure | RPC/secret | medium | mitigate | ORKA_OPERATOR_SECRET used only for signing; never logged; RPC over TLS |
| T-01-SC | Tampering | cargo installs | high | mitigate | Pin `stellar-xdr`, `stellar-strkey`, `ed25519-dalek`, `reqwest`; blocking human checkpoint on new crate |
</threat_model>

<verification>
- `cargo test --manifest-path services/Cargo.toml stellar` all green.
- fee_bump sponsorship parses; release_funds multi-sig has 2 signatures; Mode B returns XDR with no user signature.
</verification>

<success_criteria>
`stellar.rs` builds escrow txns, wraps them in an operator-sponsored fee_bump, submits to RPC, enforces multi-sig release, and returns Mode B XDR — satisfying PH1-04.
</success_criteria>

<output>
Create `.planning/phases/01-rust-backend-services/01-04-SUMMARY.md` when done.
</output>
